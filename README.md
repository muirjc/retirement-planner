# RP

Single-user retirement planning tool: longevity, tax optimization, and location comparison.

There is no CLI or notebook entry point yet (`retirement_planner` is a library — see `docs/remaining_scope.md` §2). To see it run end-to-end against the reference scenario:

```bash
pip install -e .
python examples/reference_scenario.py
```
